import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        primary: {
            main: "#bb2c24",
        },
        secondary: {
            main: "#000000",
        },
        error: {
            main: "#EE2A74",
        },
    },
});

export default theme;